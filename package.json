{
  "name": "last-orbit",
  "version": "2.0.0",
  "private": true,
  "description": "Dependency-free Persian space survival game",
  "scripts": {
    "test": "npm run test:unit && playwright test",
    "test:ui": "playwright test --ui",
    "test:unit": "node --test tests/unit/*.test.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
