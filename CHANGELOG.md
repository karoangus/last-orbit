# تغییرات

## 2.0.0 — 2026-09-19

### گیم‌پلی و تجربهٔ کاربری
- اضافه‌شدن مسیر کوتاه در کنار سفر اصلی، دو نقطهٔ پشتیبانی با انتخاب پاداش، نقشهٔ مسیر و امتیاز/رکورد هر مسیر.
- محافظ اختیاری دمای موتور، منوی توقف، توقف خودکار با خروج فوکوس/پنهان‌شدن صفحه، حالت سبک و ذخیرهٔ تنظیمات صدا/حرکت/مسیر.
- بازطراحی شروع، راهنمای بازشونده، کارت پایان، کنترل‌های موبایل و دسترسی کیبورد به خدمه و سربرگ بحران‌ها.

### رفع اشکالات
- حذف ۲۳ نسخهٔ تکراری تابع کارت خدمه و ۲۳ فراخوانی اضافهٔ autoplay در هر فریم؛ زمان‌بندی autoplay اکنون بر اساس dt واقعی است.
- اصلاح شمارش معکوس ۳←۲←۱، محدودکردن شتاب با cooldown در خود تابع و جلوگیری از ربودن Space از دکمه‌های دیگر.
- قطع مأموریت هنگام مصدومیت/قفل دیگر assignment سرگردان باقی نمی‌گذارد؛ فراخوانی مسیر حرکت را هم پاک می‌کند.
- قفل‌های متعلق به یک بحران هنگام بسته‌شدن آن آزاد می‌شوند. پایان استراحت خدمه را واقعاً آزاد می‌کند.
- دیدن ایستگاه مأموریت خدمهٔ مشغول را متوقف نمی‌کند؛ تعویض فرماندهٔ تیم دونفره اندازهٔ تیم را حفظ می‌کند و دوباره هزینه نمی‌گیرد.
- رسیدگی جداگانه به مصدوم‌های متعدد، همگام‌سازی کارت خدمه پس از تغییر وضعیت، رفع ناهماهنگی احتمال موفقیت در UI و محاسبات.
- دکمه‌های گزینه دقیقاً در آستانهٔ هزینهٔ خودشان فعال می‌شوند؛ حفظ موقعیت اسکرول و فوکوس گزینه هنگام بازسازی کارت.
- محاسبهٔ تدریجی دوبارهٔ buffها/سیستم‌ها، اصلاح واحد مصرف اکسیژن در دقیقه و پاک‌سازی کامل neglect/اعلان‌ها/رکورد رویدادهای سفر در retry.
- توصیف تصادفی بحران دیگر RNG بازی را با هر رندر تغییر نمی‌دهد. شمارندهٔ رویداد نادر به آخرین ۹ رویداد محدود نیست.
- شروع بازی بدون WebAudio یا localStorage نیز کار می‌کند؛ وعدهٔ ناموفق بررسی آپدیت PWA مدیریت می‌شود.

### کارایی و آفلاین
- fixed timestep مستقل از نرخ نمایشگر، توقف کار در پس‌زمینه، HUD محدودشده و حذف layout read از حلقهٔ تک‌تک خدمه.
- جایگزینی transition طولانی و عقب‌افتادهٔ خدمه با حرکت transform مبتنی بر زمان شبیه‌سازی.
- cache مختص scope بازی، محدودکردن cache به دارایی‌های شناخته‌شده، fallback آفلاین برای خطای سرور، timeout شبکه و نگه‌داشتن worker تا پایان cache write.
- اعلان آپدیت صفحه را خودکار reload نمی‌کند؛ reload دستی دربارهٔ شروع مجدد سفر هشدار می‌دهد.

### اعتبارسنجی
- ۴۶ تست مرورگر (۲۳ سناریو × دسکتاپ/موبایل) + ۶ تست واحد سرویس‌ورکر.
- بررسی هر ۶۱ تعریف رویداد و گزینه‌های تمام مراحل؛ شش ترکیب seed/مسیر در هر پروفایل با پایان سفر و بررسی invariantهای منابع و مالکیت خدمه.
- بررسی چیدمان ۳۶۰×۶۴۰ و ۸۴۴×۳۹۰، شروع/پایان/توقف، مسیر آفلاین و storage خراب یا غیرفعال.
- نمونه‌برداری محلی Chromium در viewport برابر ۳۹۰×۸۴۴ با چهار بحران: ۱۸۰ فریم، میانه و صدک ۹۵ فاصلهٔ فریم تقریباً ۱۶٫۷ms؛ مجموع script حدود ۳۱ms و layout حدود ۱۸ms در آن بازهٔ سه‌ثانیه‌ای. این اندازه‌گیری آزمایشگاهی است، نه تضمین نرخ فریم روی تمام دستگاه‌ها.
- HTML فشرده با gzip از حدود ۵۵٫۷KB به ۶۱٫۹KB رسیده؛ کتابخانه، فونت یا فایل رسانه‌ای جدید برای اجرای بازی لازم نیست. ابزارهای npm فقط وابستگی توسعه/تست‌اند.
